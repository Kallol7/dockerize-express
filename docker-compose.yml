services:
  nginx:
    image: nginx:stable
    volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "8080:80"
    depends_on:
      - express-app
  
  express-app:
    build: .
    expose:
      - 5000
